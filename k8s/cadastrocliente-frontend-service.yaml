apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  labels:
    app: cadastrocliente-frontend
spec:
  selector:
    app: cadastrocliente-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: NodePort
