version: '3.8'

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    image: cadastro-cliente-frontend:latest
    container_name: cadastro-cliente-frontend
    ports:
      - "3000:80"
    restart: unless-stopped
